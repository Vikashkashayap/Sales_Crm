# Legacy welcome kit folder

**Preferred location:** `backend/uploads/onboarding/` (manage via **Settings → Document Management**).

Files here are still used as a fallback if the new folder has no upload yet.

The welcome email uses the student's **exam medium** (`English` or `Hindi`) from admission.
